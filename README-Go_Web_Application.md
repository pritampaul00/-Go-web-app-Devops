# Go Web Application

A simple web application built with Golang using the `net/http` package to handle and serve HTTP requests.

## Getting Started

### Prerequisites

Make sure Go is installed on your system.

### Run the Application

Start the server by running:

```bash
go run main.go
```

The application will start on port `8080`.

Open your browser and navigate to:

```text
http://localhost:8080/courses
```

to access the application.

## Application Preview

The website interface looks as shown below:

![Website](static/images/golang-website.png)
